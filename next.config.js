/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "q7qmaca0bfg5bk4o.public.blob.vercel-storage.com",
      },
    ],
  },
};

module.exports = nextConfig;
